spring:
  application:
    name: user-api
  cloud:
    stream:
      bindings:
        userCreated:
          destination: user-created
      kafka:
        streams:
          binder:
            brokers: ${KAFKA_BROKER}
    config:
      uri: ${CONFIG_SERVER}
