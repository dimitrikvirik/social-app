spring:
  application:
    name: user-api
  cloud:
    config:
      uri: ${CONFIG_SERVER}
eureka:
  instance:
    ip-address: 10.241.132.203
    prefer-ip-address: true
minio:
  url: http://10.241.76.20:9000
  accessKey: minio
  secretKey: minioPass
  bucket: storage
