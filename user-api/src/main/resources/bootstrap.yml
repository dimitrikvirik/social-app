spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${keycloak.auth-server-url}/realms/${keycloak.realm}
  application:
    name: user-api
  cloud:
    config:
      uri: ${CONFIG_SERVER}
eureka:
  instance:
    ip-address:  10.241.132.203
    prefer-ip-address: true
